import React from 'react';
import css from './index.scss';
import withStyles from 'isomorphic-style-loader/withStyles';


const Logo = () => (
    <div className='logo'>logo</div>
);

export default withStyles(css)(Logo);
